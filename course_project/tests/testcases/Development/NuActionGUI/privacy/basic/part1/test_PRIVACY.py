"""
=====================================================================
COPYRIGHT NOTICE
Authors: Hoang Nguyen, Srdan Krstic, David Basin
University: ETH Zurich
Course: Security Engineering 
This code was generated by an automated system.
=====================================================================
"""

from typing import final
from flask import current_app, Response
from flask_login import login_user, AnonymousUserMixin
from flask_user import current_user
from dtm import *
from project import *
from instrumentation import Restrict, PrivacyException
from flask_principal import Identity, identity_changed, AnonymousIdentity
from implicits import implicits
from werkzeug.datastructures import MultiDict

RESTRICTED = Restrict()

def find_personal_data(resource, subresource):
    pds = list(PersonalData.query.all())
    for pd in pds:
        if pd.resource == resource and pd.subresource == subresource:
            return pd
    return None

def add_consent(user, purpose, resource, subresource):
    pd = find_personal_data(resource, subresource)
    p = Purpose.query.filter_by(name=purpose).first()
    c = Consent(data=pd, user=user)
    c.purposes.append(p)
    db.session.add(c)
    db.session.commit()

# This test suite is dedicated for PRIVACY tests

# Test Case 8: Read other Person’s name
# on endpoint users() 
# Should fail
def test_case_8_users(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner = Person.query.filter(Person.testid == 'p1').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(owner, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test users()
        try:
            from project import users
            result = users()
            assert result['other_user_name'] != RESTRICTED
            assert result['other_user_name'] is not None
            assert result['other_user_name'] == 'p1'
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 10: Read one’s own name
# on endpoint user() 
# Should fail
def test_case_10_user(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test user()
        try:
            from project import user
            result = user(4)
            assert result['user_name'] != RESTRICTED
            assert result['user_name'] is not None
            assert result['user_name'] == caller.name
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 13: Read other Person’s surname
# on endpoint user() 
# Should fail
def test_case_13_user(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner = Person.query.filter(Person.testid == 'p1').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(owner, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test user()
        try:
            from project import user
            result = user(5)
            assert result['user_surname'] != RESTRICTED
            assert result['user_surname'] is not None
            assert result['user_surname'] == 'p1'
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

