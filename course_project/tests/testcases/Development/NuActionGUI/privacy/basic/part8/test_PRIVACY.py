"""
=====================================================================
COPYRIGHT NOTICE
Authors: Hoang Nguyen, Srdan Krstic, David Basin
University: ETH Zurich
Course: Security Engineering 
This code was generated by an automated system.
=====================================================================
"""

from typing import final
from flask import current_app, Response
from flask_login import login_user, AnonymousUserMixin
from flask_user import current_user
from dtm import *
from project import *
from instrumentation import Restrict, PrivacyException
from flask_principal import Identity, identity_changed, AnonymousIdentity
from implicits import implicits
from werkzeug.datastructures import MultiDict

RESTRICTED = Restrict()

def find_personal_data(resource, subresource):
    pds = list(PersonalData.query.all())
    for pd in pds:
        if pd.resource == resource and pd.subresource == subresource:
            return pd
    return None

def add_consent(user, purpose, resource, subresource):
    pd = find_personal_data(resource, subresource)
    p = Purpose.query.filter_by(name=purpose).first()
    c = Consent(data=pd, user=user)
    c.purposes.append(p)
    db.session.add(c)
    db.session.commit()

# This test suite is dedicated for PRIVACY tests

# Test Case 292: Read a Category’s subscribers’s name as the moderator of the category
# on endpoint view_category() 
# Should fail
def test_case_292_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(owner, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(13)  
            assert result['category_subscriber_names'] != RESTRICTED
            assert len(result['category_subscriber_names']) == 1
            assert set(result['category_subscriber_names']) == set(['p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 294: Read a Category’s subscribers’s surname as the moderator of the category
# on endpoint view_category() 
# Should fail
def test_case_294_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(owner, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(13) 
            assert result['category_subscriber_surnames'] != RESTRICTED
            assert len(result['category_subscriber_surnames']) == 1
            assert set(result['category_subscriber_surnames']) == set(['p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 316: Read a Category’s events’s owner’s name as the moderator of the category
# on endpoint view_category() 
# Should fail
def test_case_316_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(13)  
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 317: Read a Category’s events’s owner’s name as non-moderator, as owner
# on endpoint view_category() 
# Should fail
def test_case_317_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 318: Read a Category’s events’s owner’s name as non-moderator, as manager
# on endpoint view_category() 
# Should fail
def test_case_318_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 319: Read a Category’s events’s owner’s name as non-moderator, as attendant
# on endpoint view_category() 
# Should fail
def test_case_319_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14) 
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 320: Read a Category’s events’s owner’s name as non-moderator, as requester
# on endpoint view_category() 
# Should fail
def test_case_320_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 321: Read a Category’s events’s owner’s name as non-moderator, as stranger
# on endpoint view_category() 
# Should fail
def test_case_321_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'name')
            add_consent(owner1, CORE, 'Person', 'name')
            add_consent(owner2, CORE, 'Person', 'name')
            add_consent(owner3, CORE, 'Person', 'name')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14) 
            assert result['category_event_owner_names'] != RESTRICTED
            assert len(result['category_event_owner_names']) == 7
            assert set(result['category_event_owner_names']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 322: Read a Category’s events’s owner’s surname as the moderator of the category
# on endpoint view_category() 
# Should fail
def test_case_322_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(13)  
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()


# Test Case 323: Read a Category’s events’s owner’s surname as non-moderator, as owner
# on endpoint view_category() 
# Should fail
def test_case_323_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14) 
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 324: Read a Category’s events’s owner’s surname as non-moderator, as manager
# on endpoint view_category() 
# Should fail
def test_case_324_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 325: Read a Category’s events’s owner’s surname as non-moderator, as attendant
# on endpoint view_category() 
# Should fail
def test_case_325_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 326: Read a Category’s events’s owner’s surname as non-moderator, as requester
# on endpoint view_category() 
# Should fail
def test_case_326_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()

# Test Case 327: Read a Category’s events’s owner’s surname as non-moderator, as stranger
# on endpoint view_category() 
# Should fail
def test_case_327_view_category(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'MODERATOR0').one()
    owner1 = Person.query.filter(Person.testid == 'FREEUSER0').one()
    owner2 = Person.query.filter(Person.testid == 'ADMIN0').one()
    owner3 = Person.query.filter(Person.testid == 'p0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        try:        
            add_consent(caller, CORE, 'Person', 'surname')
            add_consent(owner1, CORE, 'Person', 'surname')
            add_consent(owner2, CORE, 'Person', 'surname')
            add_consent(owner3, CORE, 'Person', 'surname')
        except Exception:
            assert False
        # Test view_category()
        try:
            from project import view_category
            result = view_category(14)  
            assert result['category_event_owner_surnames'] != RESTRICTED
            assert len(result['category_event_owner_surnames']) == 7
            assert set(result['category_event_owner_surnames']) == set(['FREEUSER0', 'MODERATOR0', 'ADMIN0', 'p0'])
        except PrivacyException:
            assert False
        finally:
            db.session.rollback()