"""
=====================================================================
COPYRIGHT NOTICE
Authors: Hoang Nguyen, Srdan Krstic, David Basin
University: ETH Zurich
Course: Security Engineering 
This code was generated by an automated system.
=====================================================================
"""

from typing import final
from flask import current_app, Response
from flask_login import login_user, AnonymousUserMixin
from flask_user import current_user
from dtm import db
from project import *
from instrumentation import Restrict, PrivacyException
from flask_principal import Identity, identity_changed, AnonymousIdentity
from implicits import implicits
from werkzeug.datastructures import MultiDict

RESTRICTED = Restrict()

# This test suite is dedicated for PRIVACY tests

# Test Case 234: Remove other attendants from an Event that one owns
# on endpoint remove_attendee() 
# Should fail
def test_case_234_remove_attendee(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test remove_attendee()
        try:
            from project import remove_attendee
            remove_attendee(5,10)
        except PrivacyException:
            assert True
        finally:
            assert 5 in [attendee.id for attendee in Event.query.get(10).attendants]
            db.session.rollback()

# Test Case 235: Remove other attendants from an Event that one manages
# on endpoint remove_attendee() 
# Should fail
def test_case_235_remove_attendee(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test remove_attendee()
        try:
            from project import remove_attendee
            remove_attendee(5,11)
        except PrivacyException:
            assert True
        finally:
            assert 5 in [attendee.id for attendee in Event.query.get(11).attendants]
            db.session.rollback()

# Test Case 241: Accept an attendance request for an Event as an owner
# on endpoint accept_request() 
# Should fail
def test_case_241_accept_request(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test accept_request()
        try:
            from project import accept_request
            result = accept_request(7,10)
        except PrivacyException:
            assert True
        finally:
            assert 7 in [requestor.id for requestor in Event.query.get(10).requesters]
            assert 7 not in [attendee.id for attendee in Event.query.get(10).attendants]
            db.session.rollback()

