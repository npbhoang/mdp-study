"""
=====================================================================
COPYRIGHT NOTICE
Authors: Hoang Nguyen, Srdan Krstic, David Basin
University: ETH Zurich
Course: Security Engineering 
This code was generated by an automated system.
=====================================================================
"""

from typing import final
from flask import current_app, Response
from flask_login import login_user, AnonymousUserMixin
from flask_user import current_user
from dtm import db
from project import *
from instrumentation import Restrict, PrivacyException
from flask_principal import Identity, identity_changed, AnonymousIdentity
from implicits import implicits
from werkzeug.datastructures import MultiDict

RESTRICTED = Restrict()

# This test suite is dedicated for PRIVACY tests

# Test Case 166: Read an Event’s attendants’ name field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_166_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_attendant_names']) == 0 or result['event_attendant_names'][0] == RESTRICTED 
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 167: Read an Event’s attendants’ name field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_167_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_attendant_names']) == 0 or (
                result['event_attendant_names'][0] == RESTRICTED and
                result['event_attendant_names'][1] == RESTRICTED and
                result['event_attendant_names'][2] == RESTRICTED and
                result['event_attendant_names'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 168: Read an Event’s attendants’ name field that one attends
# on endpoint manage_event() 
# Should fail
def test_case_168_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(11)
            assert len(result['event_attendant_names']) == 0 or (
                result['event_attendant_names'][0] == RESTRICTED and
                result['event_attendant_names'][1] == RESTRICTED and
                result['event_attendant_names'][2] == RESTRICTED and
                result['event_attendant_names'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 169: Read an Event’s attendants’ name field that one requests
# on endpoint manage_event() 
# Should fail
def test_case_169_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(12)
            assert len(result['event_attendant_names']) == 0 or (
                result['event_attendant_names'][0] == RESTRICTED and
                result['event_attendant_names'][1] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 170: Read an Event’s attendants’ name field that one does not own, manage, attend, or request
# on endpoint manage_event() 
# Should fail
def test_case_170_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(13)
            assert len(result['event_attendant_names']) == 0 or (
                result['event_attendant_names'][0] == RESTRICTED and
                result['event_attendant_names'][1] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 171: Read an Event’s attendants’ surname field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_171_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_attendant_surnames']) == 0 or (
                result['event_attendant_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 172: Read an Event’s attendants’ surname field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_172_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_attendant_surnames']) == 0 or (
                result['event_attendant_surnames'][0] == RESTRICTED and
                result['event_attendant_surnames'][1] == RESTRICTED and
                result['event_attendant_surnames'][2] == RESTRICTED and
                result['event_attendant_surnames'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 173: Read an Event’s attendants’ surname field that one attends
# on endpoint manage_event() 
# Should fail
def test_case_173_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(11)
            assert len(result['event_attendant_surnames']) == 0 or (
                result['event_attendant_surnames'][0] == RESTRICTED and
                result['event_attendant_surnames'][1] == RESTRICTED and
                result['event_attendant_surnames'][2] == RESTRICTED and
                result['event_attendant_surnames'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 174: Read an Event’s attendants’ surname field that one requests
# on endpoint manage_event() 
# Should fail
def test_case_174_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(12)
            assert len(result['event_attendant_surnames']) == 0 or (
                result['event_attendant_surnames'][0] == RESTRICTED and
                result['event_attendant_surnames'][1] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 175: Read an Event’s attendants’ surname field that one does not own, manage, attend, or request
# on endpoint manage_event() 
# Should fail
def test_case_175_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(13)
            assert len(result['event_attendant_surnames']) == 0 or (
                result['event_attendant_surnames'][0] == RESTRICTED and
                result['event_attendant_surnames'][1] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 181: Read an Event’s managedBy’ name field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_181_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_manager_names']) == 0 or (
                result['event_manager_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 182: Read an Event’s managedBy’ name field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_182_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_manager_names']) == 0 or (
                result['event_manager_names'][0] == RESTRICTED and
                result['event_manager_names'][1] == RESTRICTED and
                result['event_manager_names'][2] == RESTRICTED and
                result['event_manager_names'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 183: Read an Event’s managedBy’ name field that one attends
# on endpoint manage_event() 
# Should fail
def test_case_183_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(11)
            assert len(result['event_manager_names']) == 0 or (
                result['event_manager_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 184: Read an Event’s managedBy’ name field that one requests
# on endpoint manage_event() 
# Should fail
def test_case_184_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(12)
            assert len(result['event_manager_names']) == 0 or (
                result['event_manager_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 185: Read an Event’s managedBy’ name field that one does not own, manage, attend, or request
# on endpoint manage_event() 
# Should fail
def test_case_185_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(13)
            assert len(result['event_manager_names']) == 0 or (
                result['event_manager_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 186: Read an Event’s managedBy’ surname field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_186_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_manager_surnames']) == 0 or (
                result['event_manager_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 187: Read an Event’s managedBy’ surname field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_187_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_manager_surnames']) == 0 or (
                result['event_manager_surnames'][0] == RESTRICTED and
                result['event_manager_surnames'][1] == RESTRICTED and
                result['event_manager_surnames'][2] == RESTRICTED and
                result['event_manager_surnames'][3] == RESTRICTED
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 188: Read an Event’s managedBy’ surname field that one attends
# on endpoint manage_event() 
# Should fail
def test_case_188_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(11)
            assert len(result['event_manager_surnames']) == 0 or (
                result['event_manager_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 189: Read an Event’s managedBy’ surname field that one requests
# on endpoint manage_event() 
# Should fail
def test_case_189_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(12)
            assert len(result['event_manager_surnames']) == 0 or (
                result['event_manager_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 190: Read an Event’s managedBy’ surname field that one does not own, manage, attend, or request
# on endpoint manage_event() 
# Should fail
def test_case_190_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(13)
            assert len(result['event_manager_surnames']) == 0 or (
                result['event_manager_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 196: Read an Event’s requesters’ name field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_196_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_requester_names']) == 0 or (
                result['event_requester_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 197: Read an Event’s requesters’ name field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_197_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_requester_names']) == 0 or (
                result['event_requester_names'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 201: Read an Event’s requesters’ surname field that one owns
# on endpoint manage_event() 
# Should fail
def test_case_201_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(9)
            assert len(result['event_requester_surnames']) == 0 or (
                result['event_requester_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()

# Test Case 202: Read an Event’s requesters’ surname field that one manages
# on endpoint manage_event() 
# Should fail
def test_case_202_manage_event(client, app_with_data):
    caller = Person.query.filter(Person.testid == 'ADMIN0').one()
    with app_with_data.test_request_context():
        login_user(caller)
        # Test manage_event()
        try:
            from project import manage_event
            result = manage_event(10)
            assert len(result['event_requester_surnames']) == 0 or (
                result['event_requester_surnames'][0] == RESTRICTED 
            )
        except PrivacyException:
            assert True
        finally:
            db.session.rollback()
